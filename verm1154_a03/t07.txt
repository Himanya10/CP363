SELECT b.broadDesc
FROM broad b
WHERE NOT EXISTS (
    SELECT 1
    FROM narrow n
    JOIN memberNarrow mn ON n.narrowId = mn.memberNarrowNarrowId
    WHERE n.narrowBroadId = b.broadId
)
ORDER BY b.broadDesc;
